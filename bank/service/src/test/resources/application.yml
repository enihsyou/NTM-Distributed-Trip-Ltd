spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_bank_service?useSSL=false
    username: root
    password: enihsyou

  jpa:
    show-sql: true
    generate-ddl: true
    properties:
      hibernate:
        use_sql_comments: true
        format_sql: true
        globally_quoted_identifiers: true
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
